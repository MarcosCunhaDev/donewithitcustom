import styled from 'styled-components/native';

const ContainerInputs = styled.View`
  gap:20px;
`;

const ContainerButton = styled.View`
  margin-top: 40px;
`;

export { ContainerInputs, ContainerButton };