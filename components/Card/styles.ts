import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  cardContainer: {
    width: '100%',
    backgroundColor: '#fff',
    borderRadius: 10,
    elevation: 1,
    marginBottom: 20,
    overflow: 'hidden',
  },
  rowContent: {
    padding: 10,
  },
  title: {
    fontSize: 16,
    fontWeight: 'bold',
    marginBottom: 5,
  },
  subTitle: {
    fontSize: 16,
    color: '#4ECDC4',
    fontWeight: 'bold',
  },
});

export default styles;
